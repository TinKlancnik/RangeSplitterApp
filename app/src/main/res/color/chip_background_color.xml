<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Selected -->
    <item android:state_checked="true" android:color="@color/colorActive"/>  <!-- semi-transparent purple -->

    <!-- Pressed -->
    <item android:state_pressed="true" android:color="@color/buttonDefault"/>

    <!-- Hovered (API ≥ 26) -->
    <item android:state_hovered="true" android:color="@color/buttonHover"/>

    <!-- Default -->
    <item android:color="@color/colorInactive"/>
</selector>